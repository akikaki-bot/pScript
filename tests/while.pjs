fn isPrime( n ){
    if( n == 1 ) return false;
    let i = 2;
    while( i < n ){
        if( n % i == 0 ) return false
        i = i + 1
    }
    return true
}

let j = 0;
while( j < 1000 ){
    j = j + 1 
    let prime = isPrime( j );
    if( prime ) print(j)
}

