# basic test module
# writte in PScript

fn testEqual( val1, val2 ){
    # if( val1 == val2 ) return true;
    if( val1 == val2 ) return true
    return false;
}

fn expect( ex, a ){
    return testEqual( ex, a );
}

